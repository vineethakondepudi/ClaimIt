import { DataPropertyPipePipe } from './data-property-pipe.pipe';

describe('DataPropertyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataPropertyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
